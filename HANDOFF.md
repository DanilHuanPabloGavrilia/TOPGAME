# Dealer's Gambit — состояние работ

Рабочая записка для продолжения в новой сессии. Обновлена на момент `ff6f34f` — пришёл
**третий отказ модерации**, восемь замечаний; четыре кода закрыты и проверены замерами,
четыре — поля и графика витрины, их закрывает владелец. Игра **не на проверке**: черновик
вернулся, правки заливаются без отправки на модерацию.

Название игры теперь одно и то же везде: **`Dealer's Gambit`**. Это не косметика, это
требование 5.1.3, и на нём завернули.

---

## Что делается прямо сейчас

Черновик вернулся с третьего отказа. План: залить исправленную сборку **без отправки на
модерацию**, переделать тексты и графику витрины, подать позже.

1. Влить `fix/moderation-1` в `main`. Только его — эффект выстрела вливается отдельно и
   после вердикта, см. «Порядок слияния».
2. Собрать и упаковать:
   ```
   npm run build
   powershell -File scripts/pack-release.ps1
   ```
   Это перезапишет `dist` и `.zip`, то есть локальную копию поданного архива. Теперь можно:
   проверка кончилась, а из тега `yandex-moderation-1` всё воспроизводится побайтно.
3. В консоли: названия в обоих слотах → `Dealer's Gambit`, тексты из `STORE.md`.
4. Графика: обложка, скриншоты, рекламное видео — с одним названием, по-английски в
   английских слотах, **11 карт вместо 12**.

---

## Где что запускается

| | Адрес | Команда | Кто трогает |
|---|---|---|---|
| **Игра (продакшен-сборка)** | http://127.0.0.1:5555 | `npx vite preview --port 5555 --host 127.0.0.1` | владелец проекта |
| Дев-сервер | http://localhost:5173 | `npm run dev` | агент, для тестов |

**Важно:** сохранения в браузере привязаны к origin. `localhost` и `127.0.0.1` — разные
хранилища. На 5555 нельзя чистить `localStorage` и подкладывать тестовые сейвы: там играет
владелец. Всё тестирование — на 5173, там есть дев-хук `window.gameState`
(в продакшен-сборку не попадает, обёрнут в `import.meta.env.DEV`).

Сбросить прогресс: `localStorage.clear(); location.reload()`

---

## Git

- Репозиторий: `github.com/DanilHuanPabloGavrilia/TOPGAME`, ветка `main`
- Закоммичено:
  - `dd846eb` — починка выстрела диллера, сохранения, сборка под площадки
  - `2e2daeb` — шкала ×10, ИИ играет всеми картами, реклама за фишки
  - `307892b` — обучение с тренером, адаптивная вёрстка
  - `891b3a4` — экономика
  - `6fefff0` — множители по тексту карт, один усилитель на выстрел, звуки
  - `b4b1b9e` — тренер ILSHMONSTER со своим портретом
  - `f76ce59` — SDK Яндекса грузится из кода; ядро локализации, карты, боссы
  - `9243136` — все боевые сообщения на 5 языках; фикс подстановки на поражении
  - `d631f81` — шаги обучения на 5 языках
  - `9a387ca` — интерфейс и статика на 5 языках, требование 2.14, реклама в LOCAL, GameplayAPI
  - `4827484` — `scripts/pack-release.ps1`, упаковка архива с проверкой раскладки
  - `8aa853d` — SDK запрашивается и вне фрейма (гейт по хосту, не по фрейму)
  - `16e78c2` — тишина во время рекламы и в фоне, контекстное меню
  - `b3bca5f` — рука на альбомном телефоне, возврат звука после рекламы
  - `45a875d` — зона диллера сделана константой, двухколоночная альбомная раскладка
  - `8d07274` — карта мира без HP и подписей статуса
  - `64ff407` — шапка без руководства в бою, кнопки досье над описанием
  - `a013de3` — эта записка (`main` стоит здесь)

### Ветки и тег

`main` **намеренно не двигается** дальше `a013de3`. Из него собирается архив, который
уезжал на проверку, — и пока не решено, что подавать следующим, он остаётся точкой отсчёта.

| Ссылка | Что там |
|---|---|
| `main` → `a013de3` | состояние, с которого собран поданный архив |
| тег `yandex-moderation-1` → `64ff407` | **что именно смотрел модератор** |
| `fix/moderation-1` → `ff6f34f` | ответ на третий отказ + тексты витрины |
| `feat/shot-fx` → `b480f22` | эффект выстрела, ждёт своей очереди |

Всё запушено на `origin`.

**Тег важнее, чем кажется.** Первый отказ разбирали по датам файлов, потому что поданное
состояние нигде не было записано. Теперь оно записано, а **сборка детерминирована** —
проверено: пересборка даёт `dist` побайтно, 60 из 60 файлов. Значит архив воспроизводится
из тега, и хранить сам `.zip` (он в `.gitignore`) не нужно.

**Порядок слияния.** Сначала `fix/moderation-1` → `main` → архив → подача. Эффект выстрела
вливается **после** вердикта, отдельно. Причина не в аккуратности: если следующий отказ
придёт на архив с пятью изменениями сразу, его снова будет нечем объяснить — ровно та
история, что с первым отказом.

**Архив собирать только из закоммиченного состояния.** Отказ модерации по 2.14 случился
ровно потому, что залитый архив не соответствовал коду. Проверка: пересобрать и сверить
файлы архива с `dist` по SHA-256 — расхождений должно быть ноль. Скрипт проверки одноразовый,
лежал в скретчпаде; при следующей подаче написать заново или оформить рядом с
`pack-release.ps1`.

---

## Что сделано

### Адаптивная вёрстка
4 медиа-блока в `style.css`. Рука уходит в горизонтальный скролл, барабан ужимается
(радиус патронов считается от `offsetWidth`, а не константой), шапка скроллится, магазин
в одну колонку. Ключевая ловушка, на которую дважды напоролись: у grid/flex-детей по
умолчанию `min-width: auto`, из-за чего `overflow-x` не включается — лечится
`min-width: 0` (см. `.game-table > *`, `.cards-container`, `.stats-group`).

### Обучение — `src/engine/TutorialManager.ts`
11 шагов, запуск из Руководства кнопкой «🎓 ПОКАЗАТЬ НА ПРАКТИКЕ». Класс ничего не
кеширует: `renderUI` перестраивает контейнеры через `innerHTML`, поэтому селектор
перерезолвится каждый кадр, а слушатель `actionRequired` перевешивается при подмене узла.
Подсветка — гигантский `box-shadow` (не `clip-path`), сама дырка `pointer-events: none`,
поэтому подсвеченная кнопка нажимается.

Шаги в `src/game/TutorialSteps.ts`.

### Тренер ILSHMONSTER

Портрет: мастер `assets-source/images/bosses/ilshmonster.jpeg` (1024×1024, вне git),
из него `npm run assets` делает `src/assets/images/bosses/ilshmonster.webp` — 256×256,
13.8 КБ. Мастера в сборку не попадают; прогон детерминированный, чужие картинки не
перегенерируются.

Портрет противника берётся из `dealer.avatarUrl`, а **не** индексом из `LOCATIONS`.
Раньше шапка боя лезла в матрицу 5×3, и тренер — который вне матрицы — показывал лицо
того босса, на чьём слоте стоял игрок.

### Тренировочный бой
`TRAINING_BOSS` в `BossCatalog.ts` — вне матрицы 5×3, иначе поедут индексы
`completedBosses`, карты мира и таблицы наград. 40 HP, урон 5, **карт не получает вообще**
(включая дораздачу при перезарядке барабана), первый патрон гарантированно холостой —
без этого не работает урок «выстрел в себя». Наград и прогресса не даёт, экраны победы
и поражения свои.

Кнопка «Тренировочный бой» в мета-магазине видна **только пока идёт обучение**
(`renderMetaShop` сверяется с `tutorial.isRunning()`).

### Экономика
- Поражение = 15% от награды **этого** босса (`defeatReward()` в `GameState.ts`)
- Удвоение за рекламу — на любой победе, на поражении убрано
- Цены апгрейдов квадратичные: `hpUpgradeCost`, `armorUpgradeCost`, `damageUpgradeCost`
- Холостой в себя: не больше 5 выплат за бой (`MAX_BLANK_SELF_SHOT_PAYOUTS`)
- Целевой темп: **6.24 прохождения** без рекламы, 3.12 с удвоением на каждой победе

### Звуки
`src/assets/sounds/`. Файлы подключены через `import`, поэтому замена файла с тем же
именем правок в коде не требует.

| Момент | Файл |
|---|---|
| Старт боя (обычного и тренировочного) | `round_starts.mp3` — имя с «s», так назвал владелец |
| Опустошение барабана → выдача 2 карт | `card_deal.mp3` |

---

## Множители урона — закрыто

Было: Овердрайв 2.0 давал ×4 при «×2.5» в описании. Из-за ×4 первого босса (100 HP)
убивали одним выстрелом с 3-го уровня прокачки урона — 375 $, меньше, чем даёт первая
локация. Суммирования множителей не было и нет: поле перезаписывается.

Принято два правила.

**1. Что написано на карте, то и происходит.** Числа живут в одной таблице
`MULTIPLIER_CARDS` в `ItemCatalog.ts`; из неё читают и эффект в `GameState.useItem`, и
реплика в диалоге, и описание карты (через `boosterText()`). Разъехаться они больше не могут.

| Карта | Множитель | Текст на карте |
|---|---|---|
| Ножовка | ×2 | Урон следующего выстрела — х2. |
| Овердрайв | ×3 | Урон следующего выстрела — х3. |
| Овердрайв 2.0 | ×2.5 | Урон следующего выстрела — х2.5. |

Формулировка короткая не случайно: `.card-desc` обрезается по трём строкам
(`-webkit-line-clamp: 3`), и на длинной фразе множитель уезжал на четвёртую, обрезанную
строку — пропадало ровно то число, ради которого карту и берут. Проверено: все три
описания теперь видны на лицевой стороне целиком.

**2. Один усилитель на выстрел.** Пока множитель стоит, второй усилитель сыграть нельзя:
`GameState.canUseItem` возвращает `false`, `useItem` выходит до списания карты. **Карта не
тратится** — остаётся на руке до следующего выстрела. В интерфейсе такая карта получает
класс `.locked`: серая, `cursor: not-allowed`, замок в углу, клик не проходит, в подсказке
объяснение вместо описания. Множитель снимают только выстрел и Нуллификатор.

Диллера это тоже касается: `DealerAI` и раньше не брал усилитель при
`ctx.damageMultiplier === 1`, теперь ещё и таблица у него общая с каталогом.

Порог ваншота первого босса сместился с 375 $ до 950 $ (5-й уровень урона, ×3) — это
уже середина третьей локации.

### Карты не покупаются — цен у них нет

Важно для любого будущего разговора о балансе карт: **карты нельзя купить**. Их бесплатно
и равновероятно раздаёт `getRandomItems` — 3 в начале боя и ещё 2 на каждой перезарядке
барабана. Фишки тратятся только на мета-прокачку (HP / броня / урон).

В коде оставалась ловушка: поле `cost` у карт и метод `GameState.buyShopItem`, у которого
**не было ни одного вызова**. Мёртвая машинерия читалась как настоящий магазин и один раз
уже привела к неверному выводу «Овердрайв 2.0 за 90 брать невыгодно». Удалено: метод,
поле `cost` у 11 карт и `cost?` из типа `ItemCard`. У реликов (`Relic.cost`) цена
обязательна и осталась. Сохранения не задеты — `SaveData` руку вообще не хранит.

Овердрайв ×3 и Овердрайв 2.0 ×2.5 просто выпадают одинаково часто. Это разброс, а не
перекос экономики: выбирать между ними за деньги не приходится.

### Приведено в соответствие с текстом ещё три карты

Правило «написано = так и есть» применено ко всему каталогу, не только к усилителям.
Везде правился **текст**, механика не менялась.

| Карта | Было в описании | Как на самом деле | Стало |
|---|---|---|---|
| Овердрайв | «Тройной урон (х3), но 1 урон себе при холостом» | штрафа нет нигде — в ветке холостого только сброс множителя | Урон следующего выстрела — х3. |
| Щит-Зеркало | «Отражает 1 урон обратно в противника» | отражает **весь** выстрел целиком | Отражает следующий выстрел обратно в стрелка. |
| Сигарета | «Восстанавливает 10% от максимального здоровья» | `max(10, 10%)` — на 80 HP лечит 10, то есть 12.5% | …10% от максимального здоровья, но не меньше 10. |
| Нуллификатор | «Сбрасывает урон босса и удаляет его карту» | сбрасывает **общий** множитель (можно затереть свой) и сжигает самую опасную карту | Сбрасывает множитель урона и сжигает карту противника. |

Если штраф Овердрайва нужен как механика — это отдельная задача, её никто не заказывал.

Потолок урона за выстрел (40% от макс. HP) обсуждался и **не вводился**.

---

## Сборка под площадки

### Загрузка SDK — решение развернули, площадка победила

Здесь раньше было написано обратное: «в `index.html` нет внешних скриптов, это намеренно».
Логика была честная — блокирующий тег в `<head>` делает yandex.ru единой точкой отказа для
первой отрисовки, — но **п. 1.19.1 её не разрешает**. Третий отказ пришёл в том числе за это:
SDK должен подключаться так, как показано в документации, тегом в разметке, до `YaGames.init()`.

Сейчас в `index.html` стоит `<script src="https://yandex.ru/games/sdk/v2"></script>`, а
`PlatformSDK` только читает глобал. Внедрения из кода, таймаута на загрузку и функции
`loadYandexSdk()` больше нет.

**Гейт по хосту при этом остался — но переехал с загрузки на инициализацию.** Это не
осторожность, это замер: с тегом SDK прекрасно грузится и вне площадки, `init()` проходит,
платформа становится `YANDEX`, и дальше всё сыплет `No parent to post message` в цикле —
наградная реклама и `GameplayAPI` мертвы, консоль залита. Это уехало бы на playtest-сборку
владельца, которая крутится top-level на `127.0.0.1`, где `import.meta.env.DEV` равен false.
Поэтому `isLocalHost()` жив и стоит перед `YaGames.init()`.

Вне площадки всё равно остаются 4 ошибки от самого SDK — их даёт скрипт при загрузке, до
любых наших вызовов. Статическим тегом этого не избежать, и это принято.

**Документация называет рекомендуемым относительный путь `/sdk.js`** — именно для игр,
загружаемых через консоль, то есть для нашего случая. Он был бы чище: вне площадки просто
404, ни шума, ни гейта. Не поставлен потому, что проверить не на чем: если `/sdk.js` в их
фрейме не отдаётся, игра остаётся без SDK и без определения языка — то есть возвращается
2.14. **Стоит спросить поддержку или проверить в черновике; замена — одна строка.**

**Побочный эффект соответствия:** тег блокирующий, до ответа yandex.ru страница не рисуется
вообще. На площадке это ничего не стоит — в это окно Яндекс показывает свой загрузчик
поверх фрейма (видно на видео модератора на 2.5 с). Вне площадки запрос быстро падает.

**Архив собирать не через `Compress-Archive`.** В PowerShell 5.1 он пишет пути с обратным
слэшем (`assets\index.js`), что нарушает спецификацию ZIP: распаковщик на Linux — а серверы
площадок такие — прочитает это как имя файла, а не папку, и все ассеты отдадут 404.

Это оформлено скриптом, руками больше собирать не нужно:

```
npm run build
powershell -File scripts/pack-release.ps1
```

Скрипт создаёт записи явно с `/`, затем **переоткрывает архив и проверяет**: ноль записей
с `\`, `index.html` в корне. Не сошлось — архив удаляется, а не уезжает на модерацию.
Сам файл скрипта намеренно в чистом ASCII: `.ps1` без BOM PowerShell 5.1 читает как ANSI,
и одно тире в комментарии роняет парсер (наступили).

Архив, который смотрел модератор: 60 записей, 906.7 КБ, собран из тега `yandex-moderation-1`.
Локальный `.zip` и `dist` могут быть уже перезаписаны — воспроизводятся из тега.

Исправленная сборка (`fix/moderation-1`): 60 файлов, +1.9 КБ к поданной.

## Локализация — закрыта

Пять языков: `ru` (источник и откат), `en`, `tr`, `kk`, `uz`. Ядро в `src/i18n/`.

**Как устроено.** Плоские словари по ключам, `t(key, params)` с подстановкой `{name}`.
Отсутствующий ключ отдаёт русский, а не мусор — недоделанный язык деградирует в читаемую
игру. Каталоги (`ItemCatalog`, `BossCatalog`) хранят **ключи, а не текст**: они строятся при
импорте, задолго до того как площадка сообщит язык. Текст появляется в резолверах —
`localizedItem()`, `localizedBoss()`, `localizedLocation()`, — через которые проходит всё,
что попадает на экран.

**Язык.** Площадка всегда побеждает: Яндекс — `ysdk.environment.i18n.lang`, VK — параметр
запуска `vk_language`. `?lang=xx` работает **только когда площадка молчит** (локальный
сервер, standalone) — это для вычитки переводов на релизной сборке, поведение площадок он
не трогает.

Правило выбора в `pickLang()` (`main.ts`): берём первый источник, который **вообще что-то
назвал**; если названный язык мы не возим — **английский**, не русский. Русский только когда
не назвал никто. Это требование п. 2.14 Яндекса: игрок с немецким интерфейсом Яндекса
получит английский, а не русский, которого он не прочитает.

Проверено на релизной сборке во фрейме с подставным `YaGames`:

| SDK отдаёт | Игра стартует |
|---|---|
| `tr` / `kk` / `en` / `uz` / `ru` | на этом языке |
| `de` / `uk` / `es` (не возим) | английский |
| `tr`, а в URL ещё `?lang=ru` | турецкий — площадка выигрывает |

Ручного выбора языка в интерфейсе нет и быть не должно: п. 2.14 требует именно
автоопределения. Геолокации в SDK нет — `i18n.lang` это язык интерфейса Яндекса, и
проверяют на модерации именно его.

**Шрифты — тут была реальная поломка.** Orbitron покрывает только базовую латиницу: ни
`latin-ext` (турецкие ş ğ ı İ), ни кириллицы вообще. Русские заголовки всё это время падали
на случайный системный шрифт. Добавлены подмножества Fira Code `latin-ext` и `cyrillic-ext`
(казахские ә ғ қ ң ө — это отдельный блок, в `cyrillic` их нет), +38 КБ. Заголовки теперь
`'Orbitron', 'Fira Code', monospace` — откат покрыт и одинаков на всех площадках.
Замерено по ширине глифов: Fira Code реально даёт турецкие и казахские буквы, Orbitron — нет.

| Блок | Ключей | Статус |
|---|---|---|
| Карты | 20 | готово |
| Локации и боссы (15 + тренер) | 122 | готово |
| Реплики диллера | 22 | готово |
| Боевые сообщения `GameState` | 57 | готово |
| Шаги обучения | 11 | готово |
| Интерфейс `main.ts` + хром обучения | 3 + ~60 | готово |
| Статика `index.html` | 49 узлов | готово |

**350 ключей × 5 языков = 1750 строк. Наборы ключей во всех пяти файлах совпадают
посимвольно** (сверялось не подсчётом строк, а диффом отсортированных списков ключей —
одинаковое количество ещё не значит одинаковый состав).

### Как переведена статика `index.html`

Разметка носит ключ, а заполняет её `applyStaticI18n()` из `src/i18n/dom.ts`:

- `data-i18n="key"` → `textContent`, обычные строки;
- `data-i18n-html="key"` → `innerHTML`, только абзацы руководства и модалки выхода, где
  внутри слова размечены `<strong>`. Источник — исключительно наши словари.

Всего 49 узлов: 41 + 8. **Помечать можно только то, что больше никто не пишет.** Узлы,
которые заполняет цикл отрисовки (имя диллера, полосы HP, тело модалки исхода), переведены
там, где они пишутся; повесить на них ещё и `data-i18n` — значит завести второго владельца
одной строки. У таких узлов в разметке стоит комментарий, кто именно их пишет.

Проверено, что `renderUI` их не перетирает: снимок всех 49 узлов до и после прогона по всем
экранам и фазам (меню → карта → магазин → досье → бой → выстрел → SHOP → GAMEOVER) —
0 потеряно, 0 перезаписано.

### Язык выбирается дважды

`index.html` рисуется до того, как площадка ответит. Поэтому `boot()` сначала ставит язык
по догадке (`?lang=` → `navigator.language` → `ru`) и красит статику, а когда SDK ответит и
язык отличается — ставит настоящий и красит ещё раз. `applyStaticI18n` для этого идемпотентна.
Без первого прохода турок смотрел бы на русское меню всё время подъёма SDK: ~1 с во фрейме,
до 6 с по таймауту, если площадка молчит.

Хром обучения (`Пропустить`, `Далее`, подсказка) переводится в `applyChrome()` при
`tutorial.start()`, а **не** в `buildDom()`: менеджер конструируется на импорте модуля,
задолго до `setLang()`, и текст, вшитый туда, остался бы русским для всех.

### Числа в тексте берутся из констант

`+25$` за холостой в себя и `15%` компенсации подставляются из `BLANK_SELF_SHOT_CHIPS` и
`DEFEAT_SHARE` (`STATIC_I18N_PARAMS` в `main.ts`). В руководстве было записано «+70$
компенсации» — при доле 15% это неверно для любого босса (за первого выходит 5$).
Переносить неверное число в пять языков смысла нет, поэтому формулировка стала
процентной и считается из константы. Заодно исправлена опечатка «ВЫИГРАШ» в модалке победы.

**Чем не стоит заниматься руками.** Каталог боссов переводился на ключи скриптом
(различал локацию и босса по отступу, ключ брал из `id`) — 122 замены вручную это
гарантированная опечатка.

Скрипт для `GameState` пришлось выносить в **файл**, а не писать инлайн: строки содержат
одновременно кавычки, бэктики и `${}`, и любая схема шелл-экранирования калечит хотя бы
одну из них. Семь замен из тридцати двух не совпали из-за вариантов эмодзи (селекторы
вариаций в `🛡️`, разные кодовые точки) — их пришлось добивать точечно.

**Как устроены боевые строки.** Строка хроники — целая фраза на язык
(`log.shot.player.dealer`), а не склейка подлежащего с дополнением: русскому нужен
винительный падеж, турецкому — аффикс на существительном. Из двух кусков это не собрать
так, чтобы читалось на всех пяти языках.

**Проверка после правок.** Паритет словарей — не подсчёт строк, а состав:

```
for f in en tr kk uz; do grep -o "^  '[^']*'" src/i18n/locales/$f.ts | sort > /tmp/$f.keys; done
grep -o "^  '[^']*'" src/i18n/locales/ru.ts | sort > /tmp/ru.keys
for f in en tr kk uz; do diff /tmp/ru.keys /tmp/$f.keys; done
```

Просачивание ключей и остатки русского ищутся **в живой странице**, а не в разметке: обход
DOM, пропускающий `display:none`, `visibility:hidden` **и `opacity:0`** — модалки паркуются
именно прозрачностью, и обход без этой проверки отчитается о «русском» в скрытых узлах.
Прогонять по всем экранам: меню, карта, магазин, руководство, досье, бой, подтверждение
выхода, модалки победы/поражения/тренировки/финала. На `en/tr/kk/uz` — ноль кириллицы,
на всех пяти — ноль сырых ключей.

Вычитывать релизную сборку можно через `?lang=xx` на `npx vite preview`.

**Вычитка.** Русский, английский и турецкий вычитки не требуют. Казахский и узбекский —
интерфейсные строки в порядке, но лор и жаргон боссов стоит показать носителю до публикации.

**Терминология зафиксирована по языкам** — при добавлении строк держаться её:
диллер = Dealer / Krupiye / Диллер / Diller, броня = ARMOUR / ZIRH / САУЫТ / ZIRH,
боевой-холостой = LIVE-BLANK / GERÇEK-KURU SIKI / ЖАУЫНГЕРЛІК-БОС / JANGOVAR-BO‘SH.

Имя босса нигде не склоняется: кнопка перехода в бой читается «В БОЙ: <имя>», а не
«К <имя>» — дательного падежа русскому и аффикса турецкому на подставленном имени не
сделать.

## Прочее незакрытое

- **Скриншоты и графика витрины — владелец переделывает.** Обложка (обе языковые версии),
  скриншоты и рекламное видео. Требования к размерам сверить по документации **до** съёмки —
  растянутый постфактум кадр завернут. Агент снять не может, см. «Чем нельзя проверять» ниже.
- **Названия в консоли** поменять на `Dealer's Gambit` в обоих языковых слотах, тексты
  заменить на те, что в `STORE.md`.
- **Вычитка носителем.** Казахский и узбекский интерфейс переведён, но глазами носителя не
  смотрен — как и лор боссов. До публикации показать.
- **Лимит выплат за холостой в себя** нигде не назван — ни в описании, ни в руководстве
  внутри игры. См. раздел про `STORE.md`.
- `ALL_RELICS` в `ItemCatalog.ts` — три реликвии с русскими именами, которые **никуда не
  импортируются**. Та же мёртвая машинерия, что и удалённый `buyShopItem`: не переведены
  осознанно, потому что на экран не попадают. Либо выкинуть, либо доделать реликвии.
- Автостарт обучения при первом запуске (сейчас только по кнопке)
- Липкий баннер и лидерборды не реализованы — это деньги мимо кассы, не блокер
- `card_use.mp3` весит 258 КБ при длительности 0.44 с — 18% всей сборки на один звук
- 25 МБ старых картинок остались в истории git (чистится только `filter-repo`)

Закрыто по ходу дела: кириллица в заголовках (Orbitron её не умеет вовсе — заголовки
теперь откатываются на Fira Code, см. раздел про локализацию).

## Эффект выстрела — ветка `feat/shot-fx`, в `main` не влит

Три эффекта на существующей канве `#fx-canvas`. Живут в `ParticleSystem`, потому что этот
класс уже владеет канвой и циклом отрисовки — второй владелец дрался бы за `clearRect`.

| Событие | Эффект |
|---|---|
| Боевой через стол | луч от стрелка к цели + вспышка у ствола + удар в цели |
| Боевой в себя | вспышка вверх, без луча — стволу некуда стрелять |
| Холостой | синяя вспышка, заметно меньше боевой |

**Луч рисуется кодом, а не растягиванием спрайта.** Дистанция между аватарами меняется с
раскладкой, и один спрайт, растянутый под неё, размазал бы вместе с лучом и вспышку. Поэтому
хвост — три сложенных штриха под градиентом, а от картинки берётся только левая треть
(`FLASH_SLICE`), и она никогда не масштабируется дистанцией.

**Фон у картинок не удаляется и не должен.** Отрисовка идёт через
`globalCompositeOperation = 'lighter'`: мастера лежат на чёрном, чёрное складывается с нулём
и исчезает само. Прогонять свечение через сервис удаления фона — значит съесть мягкий спад,
который и делает его светом, а не наклейкой. Мастера кладутся в `assets-source/images/fx/`,
`npm run assets` обрезает чёрную рамку (`trim`, порог 12) и жмёт в webp с сохранением
пропорций — для боссов и предметов обрезка и `fit: 'inside'` **не применяются**, отсюда флаг
`glow` в описании цели.

**Очередь выстрелов ограничена двумя.** Эффект стареет только в кадрах, когда его рисуют, а
фоновая вкладка усыпляет цикл отрисовки, пока таймер хода диллера продолжает тикать. Без
ограничения накопленное приезжает одним кадром — аддитивно это белая плита во весь экран.

**Не хватает одного мастера:** лиловый луч на чёрном (`shot-beam.png`) владелец не сохранил,
в `Downloads` лежала только версия на белом фоне, из которой свечение не вытащить. Игра при
этом работает: луч, удар и обе вспышки рисуются кодом, картинка добавит только молнии и конус
у ствола. Загрузка идёт через `import.meta.glob`, а не статическим `import`, — отсутствующий
мастер не роняет сборку.

Замерено: луч 11/11 точек на линии, 0/11 в 200 px сбоку; градиент реальный —
`(249,107,220)` у ствола, `(171,129,255)` в середине, `(135,244,255)` у цели; выстрел диллера
разворачивает луч сам, зеркалящего кода нет. Вес: +18.0 КБ.

## RuStore — разобрано, не начато

**RuStore это магазин Android-приложений, а не веб-площадка.** Нужен APK/AAB, то есть обёртка.
Рекомендация — Capacitor: весь `dist` внутрь пакета, работает офлайн, свой хостинг не нужен.
TWA потребовал бы собственный домен и интернет при каждом запуске.

Что уже работает в нашу пользу: `base: './'`, отсутствие внешних скриптов кроме SDK,
сохранения на `localStorage`, готовая мобильная вёрстка, подавление меню и выделения, глушение
звука по `visibilitychange`. И `isLocalHost()` сам отключит SDK Яндекса — Capacitor отдаёт
приложение именно с `localhost`.

Что придётся решить:
- **Реклама.** В APK платформа будет `LOCAL`, а релизная сборка в `LOCAL` рекламу
  **осознанно отказывает**. Все четыре точки наградной рекламы станут кнопками, которые
  всегда отвечают «недоступно». Либо прятать их флагом сборки, либо подключать мобильный
  рекламный SDK. Флагом — экономика уходит на медленный темп: 6.24 прохождения вместо 3.12.
- **Кнопка «Назад».** Обработки нет вообще (`popstate`/`beforeunload` в проекте не
  встречаются). На Android системный «назад» вылетит из игры посреди боя. Вешать на
  существующую модалку выхода. Нужна при любом решении, от рекламы не зависит.
- Облачных сохранений не будет — только локальные, переустановка теряет прогресс.
- Оболочка: ориентация, `safe-area`, иконка-адаптив, сплэш, keystore (**потеря ключа =
  обновлять приложение больше нельзя**), Android Studio и SDK.

**Главный риск не технический.** Возрастная маркировка и содержание: казино, ставки и
механика «выстрел в себя с выплатой». Обёртка в APK этого не решает — если содержание не
проходит, время на Capacitor и подпись потрачено зря. Решать до сборки, а не после.

**Как вести платформенную работу, не ломая основную папку.** Не ветками — `git checkout`
переписывает файлы в той же папке. `git worktree add ../dealers-gambit-rustore -b
platform/rustore` даёт вторую рабочую копию в отдельном каталоге с общей историей; основная
не меняется вообще, `git worktree remove` убирает следы.

Но постоянное место платформенного кода — **`main` за флагом сборки**, а не долгоживущая
ветка: та отстанет и превратится в разбор конфликтов в `main.ts` на 1141 строку. Флаг —
`VITE_PLATFORM`, Vite подставляет литерал и минификатор выкидывает чужую ветку целиком. Это
проверено на том же механизме: `import.meta.env.DEV` вырезает дев-хуки, `window.gameState` и
`window.particles` встречаются в релизном бандле **0 раз**. Шов уже есть и уже правильный —
`PlatformSDK` единственный файл, разговаривающий с площадками.

## Чем нельзя проверять и что делать вместо

Копится из сессии в сессию, экономит часы.

**Скриншоты не снимаются.** Панель браузера в этой среде не отображается, страница не
компонует кадры, запрос отваливается по таймауту. Следствие серьёзнее: `document.hidden`
равен `true`, **`requestAnimationFrame` заморожен**, цикл отрисовки не идёт, CSS-переходы не
продвигаются. Всё, что зависит от кадров, проверять нельзя обычным путём.

Что работает вместо:
- **Читать пиксели канвы.** `getImageData` по точкам вдоль ожидаемой линии и в стороне от
  неё — это доказательство, а не впечатление.
- **Вызывать отрисовку руками.** Приватность в TypeScript существует только при компиляции:
  `particles.drawShots()` из консоли работает, RAF для этого не нужен. Для этого в
  DEV-блоке `main.ts` выставлены `window.gameState` и `window.particles`.
- **Рассылать события и смотреть `defaultPrevented`** — так проверялись подавления меню,
  выделения и перетаскивания.
- **Читать вычисленные стили** — так ловится отсутствие вендорного префикса.
- Помнить: **`getComputedStyle` во время перехода отдаёт текущее значение**, а в замороженной
  вкладке переход не идёт вообще. Не строить логику на `transitionend`.

**Вложения модерации.** PNG читаются напрямую. MP4 — через `imageio_ffmpeg` (стоит в системе,
привозит бинарник; в PATH ffmpeg нет): разобрать на кадры, при сомнении вырезать участок
`sharp`-ом и увеличить. Мелкие расхождения в шапке иначе не разглядеть.

**Собирать на проверку — в отдельный каталог.** `npx vite build --outDir <scratch>` вместо
`npm run build`, иначе перезапишется `dist`, а с ним локальная копия поданного архива.

**`grep -c` считает строки, а не вхождения.** В минифицированном бандле это разные числа;
для подсчёта — `grep -o ... | wc -l`. Наступили на этом при проверке `draggable="false"`.

**Скрипты правки словарей писать в файл, а не инлайном.** Строки содержат кавычки, апострофы,
бэктики и `${}` одновременно, любая схема шелл-экранирования калечит хотя бы одну.

---

## Требования площадки — что закрыто и чем проверено

**Отказ модерации был по п. 2.14 (автоопределение языка).** Причина установлена по датам:
архив, лежавший в проекте, собран 7 авг в 13:27, а коммит `f76ce59` с `getLanguage()` и всем
ядром локализации — в 17:59 того же дня. Смотрели сборку, которая была на четыре с половиной
часа старше первой строчки языкового кода: там не было ни определения языка, ни переводов.
Замечание про ту версию верное.

**Реклама в `LOCAL` больше не бесплатная.** Раньше `showRewardedVideo` в `LOCAL` вызывал
`onSuccess()` безусловно — а `LOCAL` это не только «разработчик за ноутбуком», но и «SDK не
поднялся внутри настоящего фрейма». Кто мог сделать yandex.ru недоступным, получал
неограниченные награды. Теперь ветка разделена по `import.meta.env.DEV`: в дев-сборке награда
выдаётся (иначе рекламные фичи нечем тестировать), в релизной — отказ через `onError`.

Замерено на релизной сборке в `LOCAL`: четыре клика по «Реклама за фишки» — баланс 100 $ не
сдвинулся, в консоли четыре `No ad provider available, reward refused`. На дев-сервере тот же
клик даёт 100 → 118 $.

Все четыре точки вызова наградной рекламы теперь имеют `onError`, который показывает
`ui.ad.unavailable` — иначе кнопка молча ничего не делает и это читается как поломка.

**`GameplayAPI.start()/stop()` вызывается.** Переключается в `renderUI` по признаку
«экран BATTLE и фаза BATTLE», до ветвления — потому что ветки исхода запускают межстраничную
рекламу, а Яндекс ждёт `stop()` до открытия рекламы, а не после. Обе функции идемпотентны
(`gameplayRunning`), потому что цикл отрисовки дёргает их на каждой перерисовке.

Замерено на стенде, где подставной SDK записывает вызовы:

```
загрузка -> ready
досье    -> ready
дуэль    -> ready > start          (start ровно один раз)
выстрел  -> ready > start          (перерисовка не дублирует)
выход    -> ready > start > stop
```

**Стенд.** Проверять это руками неудобно, поэтому путь такой: скопировать `dist/index.html`,
вставить заглушку, определяющую `window.YaGames`, и открыть копию **не на localhost** — см.
гейт ниже. Заглушка читает `?ylang=`, чтобы гонять языки, и пишет вызовы `GameplayAPI` в
массив. Файлы стенда кладутся в `dist/` и удаляются до упаковки — в архив они не попадают
(проверяется `grep YaGames dist/index.html` = 0).

**Изменилось после третьего отказа:** настоящий тег SDK теперь стоит в `<head>` и определяет
`window.YaGames` сам. Значит заглушку надо ставить **после** него, чтобы она перезаписала
глобал, а не до. Порядок в собранном файле: тег SDK → модуль игры → стили. Проще всего
вставить заглушку сразу после тега SDK.

Работающий приём: подняться на `http://app.localhost:4173` вместо `http://localhost:4173`.
Хост другой, гейт пропускает, iframe и всё остальное работает как обычно.

**Заодно это единственный способ увидеть `LoadingAPI.ready()`** — на localhost гейт до него
не доходит, и вызов проверить нечем. При следующей подаче стенд стоит поднять и убедиться,
что `ready()` приходит ровно один раз и после снятия `#boot-overlay`.

### Второй отказ по 2.14 и гейт загрузки SDK

Замечание пришло **повторно**, уже на сборку с локализацией, — так что «залили старый архив»
второй раз не объясняло. Нашлось вот что: SDK грузился по условию `isFramed()`, то есть
**игра, открытая своим URL, к SDK не обращалась вообще**. Проверка «определяет ли игра язык
через SDK» видит игру, которая с SDK не разговаривает.

Условие снято. Теперь SDK запрашивается всегда, кроме локального хоста и VK-контекста.
Гейт именно по имени хоста, а не по `import.meta.env.DEV`, и это важно: **владелец играет на
релизной сборке на 127.0.0.1:5555**, для неё `DEV` равен false. Гейт по `DEV` заставил бы её
ждать yandex.ru и сыпать в консоль ошибки хендшейка, которому не с кем разговаривать.

Подтвердить связь этой правки с отказом нечем: на площадке игра всегда во фрейме, а там SDK
грузился и раньше. Это настоящий дефект, но не доказанная причина.

### Третий отказ — восемь замечаний, ветка `fix/moderation-1`

**П. 2.14 в списке нет.** Локализация закрыта, автоопределение языка приняли. П. 3.4
(азартные игры) в этот раз тоже не поднимали — но он и не закрыт, просто не всплыл.

Четыре замечания в коде, четыре в витрине. Вложения к письму — два PNG и три mp4; PNG
читаются напрямую, mp4 разбираются на кадры через `imageio_ffmpeg` (он уже стоит в системе
и привозит с собой бинарник ffmpeg — в PATH ffmpeg нет).

**П. 1.19.1, загрузка SDK** — см. раздел «Загрузка SDK» выше. Решение развёрнуто.

**П. 1.19, GameReady вызывается поздно.** Формулировка: «вызов GRA осуществляется после
того, как игра становится доступной для взаимодействия». Причина: `LoadingAPI.ready()`
стоял внутри `PlatformSDK.init()`, а меню рисовалось прямо из `index.html` и кликалось с
первого кадра. Сигнал всегда отставал от первого возможного тапа.

Видео это показывает покадрово: 2.5 с — загрузчик Яндекса, 3.5 с — карточка «Играть»,
**4.0 с — меню на экране, курсор уже на «ИГРАТЬ (КАРТА МИРА)», а в шапке ещё нет
`ФИШКИ: 100 $`**, 4.5 с — шапка полная. Сверено вырезкой полосы шапки из обоих кадров:
подсказка Яндекса на одном месте, счётчик фишек в одном есть, в другом нет.

Починка: `#boot-overlay` в разметке **перед** `#app`, стили в `<head>` блокируют отрисовку —
значит оверлей непрозрачен с первого кадра и игру нельзя ни увидеть, ни нажать. Снимается в
`finally` после `renderUI()`, `notifyGameReady()` стоит следующей строкой. Документация
требует, чтобы **в момент вызова экранов загрузки уже не было**, поэтому порядок именно
такой: сначала снять, потом сигналить.

Три страховки, каждая на свой отказ, и **все три обязательны**:
- оверлей снимается из `finally`, а не из счастливого пути — исключение в `boot()` иначе
  оставит игру навсегда за экраном загрузки;
- на `platformSDK.init()` стоит потолок 6 с через `Promise.race` — зависшее рукопожатие
  делало бы то же самое;
- узел **удаляется из документа**, а не гасится прозрачностью. Первая версия полагалась на
  CSS-переход, и это поймал замер: в throttled-вкладке класс применён, часы перехода не
  идут, панель висит на `opacity: 1` поверх игры, кликаемая насквозь.

Застрявший экран загрузки хуже всего, что он предотвращает.

**П. 1.6.2.7 и 1.6.1.8 — это одна ошибка, увиденная дважды, и это не выделение текста.**
На обоих видео **тянут портрет босса из досье**: на десктопе видна поднятая копия с курсором
«нельзя», на iOS — подъём по долгому нажатию с подсветкой оригинала.

Два дефекта:
- в `style.css` стоял `user-select: none` **без вендорного префикса**. Safari, а значит весь
  iOS, читает `-webkit-user-select` — отсюда «воспроизводится на iOS» при чистом десктопе;
- `contextmenu` висел на `#app`, то есть не на странице: поля вокруг, отступы и сам документ
  вне этого узла.

Сейчас: все префиксы плюс стандартное свойство, `-webkit-user-drag`, `touch-action:
manipulation` против двойного тапа, а `contextmenu`, `selectstart` и `dragstart` подавляются
**на документе**. Плюс `draggable="false"` во всех четырёх шаблонах, выводящих `<img>`.

Атрибут не для красоты: **на iOS подъём картинки — нативный жест, он может не поднимать DOM
`dragstart` вообще**, так что обработчик там ненадёжен, а атрибут работает слоем раньше.

Замерено: 24 события из 24 подавлены на восьми целях, включая `document` и `html`, куда
прежний обработчик не дотягивался; на вьюпорте 375×812 на самом портрете из iOS-скриншота —
атрибут и свойство `false`, оба `-webkit-` свойства `none`; в бою все 20 картинок на экране
несут атрибут. Отдельно проверено, что минификатор **не выбросил** префиксные свойства:
`-webkit-touch-callout`, `-webkit-user-drag`, `-webkit-user-select` все в собранном CSS.

`stopPropagation` в проекте не встречается ни разу — обработчики на документе обойти нечем,
включая узлы, созданные позже через `innerHTML`.

**Проверить нельзя: настоящий iOS.** `-webkit-touch-callout` ни один доступный движок не
отдаёт в вычисленных стилях, а нативный жест не воспроизводится рассылкой событий.

**П. 5.1.3, одно название** — было пять вариантов:

| Где | Что было |
|---|---|
| Консоль | `Dealer's Gambit: Кибер-Рогалик` |
| Шапка и главный экран | `⚡ DEALER'S GAMBIT` |
| `<title>` и `ui.title` | `Dealer's Gambit — Кибер-Оккультный Рогалик` |
| Обложка | `DEALER'S GAMBIT` + `КИБЕР-ОККУЛЬТНЫЙ РОГАЛИК` |
| Подзаголовок меню | `Кибер-Оккультная Дуэль в 5 Локациях & 15 Боссах` |

Стало `Dealer's Gambit` в девяти местах: `<title>`, шапка, главный экран, экран загрузки и
все пять словарей. **Верхний регистр перенесён в CSS** (`text-transform`), чтобы текстовый
узел остался ровно зарегистрированной строкой; молния из заголовка убрана. Турецкий и
узбекский сохранили аффиксы `Gambit'e` и `Gambit'ga` — это грамматика, а не второе название.

Апостроф тоже привели к одному: в четырёх словарях стоял `’` (U+2019), в русском `'`. Правка
делалась скриптом — десять замен вручную в пяти файлах с кириллицей и турецкими диакритиками
это гарантированная опечатка.

**П. 5.4, теги** — модератор сам снял «кликеры», делать нечего.

**П. 8.2.3, языки полей** — витрина, см. `STORE.md`.

### Витрина — `STORE.md`

Все поля черновика лежат в `STORE.md` в готовом к вставке виде. Файл в репозитории
намеренно: первый отказ разбирали по датам, потому что поданное существовало только внутри
консоли и прочитать его было негде. Тексты витрины расходятся с игрой ровно так же, как
архив расходится с кодом, и лечится это одинаково — записанным источником рядом с кодом.

**Три утверждения в поданных текстах игре не соответствовали.** Проверялись по коду, а не по
памяти:

| В описании | В игре |
|---|---|
| «12 УНИКАЛЬНЫХ КАРТ» / «12 UNIQUE ITEM CARDS» | **11** — 11 ключей в `ALL_ITEMS`, 11 файлов иконок |
| «Бонус Капитала» / «Capital Bonus» как четвёртая прокачка | Её **нет**. `MetaUpgrades` — три поля, магазин продаёт три вещи |
| «Handsaw», «Hack-Chip», «Cyber-Armor», «Shoot Dealer» | `Hacksaw`, `Hack Chip`, `Cyber-Armour`, `Shoot the Dealer` |

Цифру 12 повторяет и обложка. Имена карт брать из словарей, а не по памяти.

Ещё: русское «Об игре» несло `(ROGUELITE)` и `Max HP` непереведёнными — это и есть кандидат
на 8.2.3; оба английских поля **потеряли все переносы строк** («roguelike!You wake up»), в
русских переносы на месте.

**Записано, но в тексты не понесено:** выплата за холостой в себя ограничена **пятью разами
за дуэль** (`MAX_BLANK_SELF_SHOT_PAYOUTS`) и не начисляется в тренировочном бою. Повторный
ход безусловен, деньги — нет. Описание обещает `+25$` без оговорок, руководство внутри игры
молчит так же. Модератор, выстреливший шестым холостым, увидит ноль. Решение за владельцем:
дописать оговорку, снять лимит или оставить.

**Графика — только владелец.** Оба языковых слота обложки держат **один и тот же файл**:
две ссылки из письма скачиваются побайтно одинаковыми. Английской обложке нужно имя без
второй строки внутри титульного блока, все остальные слова по-английски и 11 карт вместо 12.

### Чек-лист перед первой публикацией

В консоли разработчика есть чек-лист из 23 пунктов, без всех галочек кнопка «Отправить на
модерацию» не активна. Технические закрыты в коде:

| Пункт | Что сделано |
|---|---|
| 1.6 контекстное меню | `contextmenu` подавляется на `#app`; для iOS в `*` добавлен `-webkit-touch-callout: none`, событие туда не достаёт |
| 1.3 звук при потере фокуса | `visibilitychange` → `sound.setSuspended()` |
| 4.7 звук во время рекламы | `beginAd()` глушит, `endAd()` возвращает |
| 4.5 согласие на rewarded | и так выполнялось: вся наградная реклама за явной кнопкой |
| 1.9 сохранения | `ysdk.getPlayer()` уже используется |

**Заглушение — отдельный флаг, не `isMuted`.** Кнопка звука рисуется и сохраняется из
`isMuted`; переиспользовать его значит перещёлкнуть кнопку под игроком и сохранить чужую
настройку. Усыпить `AudioContext` тоже мало — звук, уже играющий, продолжится, поэтому
`stopAll()` глушит и пул `HTMLAudio`.

**Сторожевой таймер рекламы снимает блокировку в обход `endAd()`** — там пришлось снимать
заглушение отдельно, иначе не открывшаяся реклама оставляла игру немой до конца сессии.

**`resume()` на мобильных требует пользовательского жеста.** Колбэк рекламы и
`visibilitychange` жестами не являются, поэтому после первой же рекламы игра замолкала
навсегда. Теперь повтор откладывается на следующий тап, и проверяется **состояние контекста**,
а не обещание `resume()` — оно умеет резолвиться, ничего не изменив.

Что закрывает не код, а владелец: возрастной рейтинг, уникальность названия, жанр и описание,
**п. 3.4 «запрещённый контент»** (в списке прямым текстом «азартные игры», а у нас казино,
ставки и рулетка с револьвером — самый вероятный источник следующего отказа),
**п. 3.6 «оригинальная разработка»** (механика близка к Buckshot Roulette),
**декларация использования ИИ** (код писался с ассистентом, портреты сгенерированы —
честный ответ «частично»), и **п. 1.14 «игра запускалась в черновике»** — гейт проверяет
факт запуска автоматически.

---

## Мобильная вёрстка боя

Отдельный раздел, потому что здесь три ловушки, на которых легко потерять день.

**Принцип: у руки игрока бюджет высоты, который нечем съесть.** Всё остальное на экране боя
подчинено этому. Пока в зоне диллера оставался хоть один элемент переменной высоты, рука
уезжала за экран — по замерам от 5 карт из 5 до 0 в зависимости от длины реплики и числа
карт у босса.

**Альбомный телефон — двухколоночная сетка.** Руку поставили *справа* от барабана, а не под
ним: по вертикали содержимому нужно ~465px при ~289 доступных, а по горизонтали 731px
простаивал. Сейчас всё влезает без прокрутки, полная рука из пяти карт — двумя рядами.

**Зона диллера сделана константой (63–64px на всех языках):**

- реплика скрыта в альбомной, ограничена двумя строками в портретной;
- полоска карт диллера свёрнута в счётчик `🎴 ×N` ниже брейкпоинта коротких экранов —
  девять рубашек по 30px это 334px, которые не сжимались и выдавливали всё остальное;
- на `.dealer-zone` стоит `max-height`, чтобы будущий контент не раздул её снова.

Скрытая реплика ничего не уносит с экрана, кроме характера боссов: урон виден всплывающей
цифрой и полосой HP, боевой/холостой — вспышкой экрана и цветом патрона, Лупа и Хак-чип
пишут результат прямо в барабан через `knownBullets`, чей ход — индикатором. **Два
сообщения были единственными носителями и переехали во всплывающий текст:** отказ сыграть
второй усилитель (на мобильном подсказка карты тоже скрыта, игрок иначе тыкает в карту и не
получает объяснения) и какой патрон выбросил Энергетик.

### Три ловушки вёрстки

**1. `min-height: auto` — та же ловушка, что `min-width`, только по вертикали.** У
`.game-table > *` стоял только `min-width: 0`. Строка `1fr` не сжималась ниже своего
содержимого и выталкивала руку за пределы `overflow: hidden`.

**2. `.screen-panel` имел `height: 100%`, будучи флекс-ребёнком под шапкой.** Колонка
выходила «шапка + целый экран», и `#app` обрезал разницу — ровно руку. Нужен `flex: 1` плюс
`min-height: 0`, а не фиксированная высота.

**3. Инлайновый `font-size` бьёт любой медиазапрос.** Имя босса оставалось 1.8rem на телефоне,
потому что размер стоял в `style=` прямо в разметке. Правило для мобильного просто не
работало. Размеры — в таблицу стилей, в разметке их быть не должно.

### Как это проверять

Мерить надо **худший случай, а не удобный**: Феликс (у него по описанию много карт),
девять карт у диллера, полная рука из пяти, самая длинная реплика из словаря, и все пять
языков — турецкий и казахский длиннее русского. Проверка «карта видна» это хит-тест
`document.elementFromPoint` по центру карты, а не наличие узла в DOM: карта может быть
отрисована и при этом обрезана контейнером.

Три вьюпорта: `731×337` (альбомный телефон, размер снят со скриншотов владельца),
`360×740` (портретный) и `1280×800` (десктоп, не должен меняться вообще).
